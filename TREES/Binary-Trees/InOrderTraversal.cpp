#include <iostream>

class TreeNode
{
    public:

        //Contructors
        TreeNode(int D) : data(D), left(NULL), right(NULL)
        {

        }

    private:
        int data;
        TreeNode* left;
        TreeNode* right; 
};

int main(int argc, char const *argv[])
{
    
    return 0;
}
